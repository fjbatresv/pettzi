# Auth API

The Auth API uses Amazon Cognito (User Pool + App Client) with JWT authorizer in API Gateway HTTP API.

- Endpoints include register, login, forgot-password, confirm-forgot-password.
- Auth flows are defined in the OpenAPI spec below.

```openapi
id: auth
```
