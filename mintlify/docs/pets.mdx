# Pets API

The Pets API manages pet metadata, ownership links, and archival.

- Authenticated with Cognito JWT.
- Endpoints: create, list, get, update, archive pets.
- Uses `@peto/domain-model` mappers and `@peto/utils-dynamo/http` responses.

```openapi
id: pets
```
