# shared-utils

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-utils` to build the library.

## Running unit tests

Run `nx test shared-utils` to execute the unit tests via [Jest](https://jestjs.io).

# @pettzi/shared-utils

## Purpose
General-purpose helpers shared across Lambdas (non-Dynamo specific).

## Responsibilities
- Utility functions used by multiple API libs

## Key deps
- Typescript/tslib; consumed by API projects

## Tests
- `npx nx test shared-utils`

## Docs
- Mintlify: referenced indirectly in API pages
